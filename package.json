{
  "name": "horaires-5",
  "version": "1.0.0",
  "description": "Gestion des horaires project",
  "scripts": {
    "backend": "npm --prefix Backend run dev",
    "frontend": "npm --prefix Frontend run dev",
    "dev": "node scripts/start-dev.mjs",
    "migrate": "npm --prefix Backend run migrate",
    "test": "npm --prefix Backend test -- --runInBand",
    "test:coverage": "npm --prefix Backend run test:coverage"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "i": "^0.3.7",
    "mysql2": "^3.18.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  },
  "overrides": {
    "brace-expansion": "^5.0.5"
  }
}
